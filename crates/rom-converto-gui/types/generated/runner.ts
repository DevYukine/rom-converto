// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { InfoResult } from "./info";
import type { ReportRecord, ReportTotals } from "./report";

/**
 * Dry-run plan for a simple one-input-one-output operation. No runner
 * operation emits it any more; every dry run plans a [`PlanLine`].
 */
export type BasicPlanData = { operation: string, input: string, output: string, };

/**
 * Classification of a CIA's ticket/TMD signer, derived from which
 * certificates and signatures validate.
 */
export type CiaLegitimacy = { "Legit": CiaLegitimacySubType } | "Piratelegit" | { "Standard": StandardSubType };

/**
 * Distinguishes a Legit CIA signed by the eShop CA (`Global`) from one
 * signed by a personalized ticket (`Personalized`).
 */
export type CiaLegitimacySubType = "Global" | "Personalized";

/**
 * Result of verifying a CIA's certificate chain, signatures, and (if
 * requested) content hashes.
 */
export type CiaVerifyResult = { legitimacy: CiaLegitimacy, ca_cert_valid: boolean, tmd_signer_cert_valid: boolean, ticket_signer_cert_valid: boolean, tmd_signature_valid: boolean, ticket_signature_valid: boolean, content_hashes_valid: boolean | null, title_id: string, console_id: number, title_version: number, details: Array<string>, compressed: boolean, };

/**
 * Input/output size comparison for a completed conversion.
 */
export type ComparisonData = { input_bytes: number, output_bytes: number, ratio_pct: number | null, input_format: string, output_format: string, output_sha1?: string, verify?: VerifyReport, };

/**
 * Verification result for a CIA or CCI/3DS input, tagged by detected format.
 */
export type CtrVerifyResult = { "format": "Cia" } & CiaVerifyResult | { "format": "Ncsd" } & NcsdVerifyResult;

/**
 * Summary of one DAT file, as listed by `/dat-files`.
 */
export type DatFileSummary = { id: string, name: string, signatureGroup: NamedRef, platform: NamedRef, company: NamedRef | null, currentVersion: string, latestDatFileImport: LatestDatFileImport | null, subset: string | null, tags: Array<string>, };

/**
 * Result of matching one local unit against the Playmatch DAT database.
 */
export type DatMatchData = { 
/**
 * `verify` or `identify`. Serialized as `match_kind` so it does not
 * collide with the `kind` tag of [`crate::runner::models::RunRow`].
 */
match_kind: string, path: string, verdict: string, match_algo: string | null, game_name: string | null, platform: string | null, signature_group: string | null, dat_file: string | null, dat_file_id: string | null, dat_version: string | null, external_ids: Array<ExternalId>, tracks: Array<DatTrackCheck>, 
/**
 * Decoded whole-image size, 0 when nothing was digested.
 */
size_bytes: number, match: GameAndRelationMatchResult | null, error: string | null, };

/**
 * Result of a `dat.rename` run: the planned or applied renames and how many
 * were renamed (or would be), skipped, or failed.
 */
export type DatRenameData = { rows: Array<DatRenameRowData>, dry_run: boolean, renamed: number, skipped: number, failed: number, };

/**
 * One planned or applied rename.
 */
export type DatRenameRowData = { from: string, to: string | null, action: string, detail: string | null, };

/**
 * Result of a `dat.scan` run: per-status counts and one row per unit in
 * walk order.
 */
export type DatScanData = { matched: number, misnamed: number, hint: number, unknown: number, unsupported: number, failed: number, rows: Array<DatScanRow>, };

/**
 * One scanned unit's classification.
 */
export type DatScanRow = { path: string, status: string, game_name: string | null, game_id: string | null, match_algo: string | null, canonical_stem: string | null, error: string | null, };

/**
 * One disc track reconciled against the matched game's file list.
 */
export type DatTrackCheck = { track: number, ok: boolean, algo: string | null, matched_file: string | null, };

/**
 * Result of a `dat.verify` run over a directory: per-verdict counts and one
 * [`DatMatchData`] per unit in walk order.
 */
export type DatVerifyData = { verified: number, hint: number, unknown: number, unsupported: number, failed: number, rows: Array<DatMatchData>, };

/**
 * FST geometry checked by the `--full` pass, plus any notes about what
 * was found.
 */
export type DolStructuralReport = { fst_offset: number, fst_size: number, fst_within_bounds: boolean, notes: Array<string>, };

/**
 * Result of verifying a GameCube disc image.
 */
export type DolVerifyResult = { game_id: string, 
/**
 * Present only for `.rvz` input.
 */
rvz_structure: RvzStructuralVerify | null, 
/**
 * Present only with `--full`.
 */
structural: DolStructuralReport | null, 
/**
 * Whole-disc SHA-1 (hex), informational, `--full` only.
 */
disc_sha1: string | null, ok: boolean, };

/**
 * One external database cross-reference (automatic or manual match with a
 * provider id).
 */
export type ExternalId = { provider: string, id: string, };

/**
 * One external database cross-reference (e.g. IGDB) for a matched game.
 */
export type ExternalMetadata = { providerName: string, matchType: string, providerId: string | null, comment: string | null, };

/**
 * A file's computed digests, one field per algorithm plus its size.
 * Fields for algorithms that were not requested are `None`.
 */
export type FileDigests = { crc32: string | null, sha1: string | null, md5: string | null, sha256: string | null, size_bytes: number, };

/**
 * Dry-run result of a `dat.fixdat` run: the DAT file, how many of its games
 * are missing locally, and how many files those games span.
 */
export type FixdatPlanData = { dat_file: DatFileSummary, total_games: number, missing_count: number, missing_files: number, output: string, };

/**
 * Result of a `dat.fixdat` run: the DAT file and what was written.
 */
export type FixdatWrittenData = { dat_file: DatFileSummary, total_games: number, missing_count: number, missing_files: number, output: string, };

/**
 * Result of a single-file `/identify/relations` lookup: match type plus
 * the matched game, platform, company, and DAT file, when known.
 */
export type GameAndRelationMatchResult = { gameMatchType: GameMatchType, game: PlaymatchGame | null, platform: PlaymatchPlatform | null, company: PlaymatchCompany | null, signatureGroup: PlaymatchSignatureGroup | null, datFile: PlaymatchDatFile | null, datFileImport: PlaymatchDatFileImport | null, gameFiles: Array<PlaymatchGameFile>, externalMetadata: Array<ExternalMetadata>, };

/**
 * How a file matched: by hash tier (SHA-256/SHA-1/MD5/CRC), by file name
 * and size only, or not at all.
 */
export type GameMatchType = "SHA256" | "SHA1" | "MD5" | "CRC" | "FileNameAndSize" | "NoMatch";

/**
 * One file's digests from a recursive `hash` run.
 */
export type HashRow = { path: string, digests: FileDigests, };

/**
 * The most recent recorded import of a DAT file.
 */
export type LatestDatFileImport = { id: string, version: string, importedAt: string, };

/**
 * An id/name pair, used across the API for platform, signature group, and
 * company references.
 */
export type NamedRef = { id: string, name: string, };

/**
 * Decrypt-only verify result for a single NCA (or NCZ, decompressed
 * first): whether its header decrypted and every present section
 * could be read back without error.
 */
export type NcaVerdict = { name: string, partition: string | null, ok: boolean, mismatched_sections: number, };

/**
 * Verification result for one NCCH partition within an NCSD image.
 */
export type NcchPartitionResult = { index: number, name: string, title_id: string, product_code: string, encrypted: boolean, ncch_magic_valid: boolean, exheader_hash_valid: boolean | null, logo_hash_valid: boolean | null, exefs_hash_valid: boolean | null, romfs_hash_valid: boolean | null, details: Array<string>, };

/**
 * Verification result for an NCSD (CCI/`.3ds`) cartridge image, covering
 * every partition.
 */
export type NcsdVerifyResult = { ncsd_magic_valid: boolean, title_id: string, partition_count: number, partitions: Array<NcchPartitionResult>, details: Array<string>, compressed: boolean, };

/**
 * Outcome of verifying every NCA in one Switch container.
 */
export type NxVerifyResult = { kind: string, ok: boolean, ncas: Array<NcaVerdict>, };

/**
 * What a dry-run planner decided to do with one input, before any file is
 * touched.
 */
export type PlanDecision = "New" | "Overwrite" | { "Rename": string } | "Skip" | "KeepValid" | "RewriteInvalid";

/**
 * One rendered line of a dry-run preview: an operation, its input/output
 * paths, and the decision made for it.
 */
export type PlanLine = { operation: string, input: string, output: string, decision: PlanDecision, media: string | null, missing_keys: string | null, };

/**
 * One planned or written `.m3u` playlist.
 */
export type PlaylistPlanData = { base_title: string, output: string, contents: string, disc_count: number, has_duplicate_numbers: boolean, };

/**
 * Result of a `playlist.write` run: the playlists that were (or would be)
 * written.
 */
export type PlaylistsData = { playlists: Array<PlaylistPlanData>, };

/**
 * A company as returned by the Playmatch API.
 */
export type PlaymatchCompany = { id: string, name: string, };

/**
 * A DAT file's identity and current version, as returned by the Playmatch API.
 */
export type PlaymatchDatFile = { id: string, name: string, platformId: string, signatureGroupId: string, currentVersion: string, subset: string | null, tags: Array<string> | null, };

/**
 * One recorded import of a DAT file version.
 */
export type PlaymatchDatFileImport = { id: string, datFileId: string, name: string, version: string, importedAt: string, };

/**
 * A game as returned by the Playmatch API.
 */
export type PlaymatchGame = { id: string, name: string, cloneOf: string | null, currentInLatestDat: boolean, lastSeenDatVersion: string | null, };

/**
 * A file entry of a DAT game, with its hashes and DAT presence.
 */
export type PlaymatchGameFile = { id: string, gameId: string, fileName: string, fileSizeInBytes: number | null, crc: string | null, md5: string | null, sha1: string | null, sha256: string | null, currentInLatestDat: boolean, lastSeenDatVersion: string | null, };

/**
 * A platform as returned by the Playmatch API.
 */
export type PlaymatchPlatform = { id: string, name: string, };

/**
 * A signature group (DAT provider grouping) as returned by the Playmatch API.
 */
export type PlaymatchSignatureGroup = { id: string, name: string, };

/**
 * One progress update emitted during a run.
 */
export type ProgressEvent = { "kind": "start", total: number, message: string, } | { "kind": "advance", delta: number, 
/**
 * Cumulative fraction done (0.0-1.0) since the preceding `Start`,
 * clamped to 1.0. `None` when the total was 0 or unknown.
 */
fraction?: number | null, } | { "kind": "finish" } | { "kind": "phase", message: string, } | { "kind": "warn", message: string, };

/**
 * Wraps a [`ComparisonData`] for the response's `data` field.
 */
export type RunComparisonData = { comparison: ComparisonData, };

/**
 * Operation-specific payload of a [`RunResponse`], one variant per
 * operation family.
 */
export type RunData = PlanLine | RunPlansData | FileDigests | Array<HashRow> | RunComparisonData | BasicPlanData | CtrVerifyResult | DolVerifyResult | RvlVerifyResult | NxVerifyResult | WupVerifyResult | XenonVerifyData | XenonConvertData | InfoResult | PlaylistsData | DatMatchData | DatVerifyData | DatScanData | DatRenameData | FixdatPlanData | FixdatWrittenData;

/**
 * Per-operation options accepted in a [`RunRequest`], validated
 * per-operation by the handler that reads them.
 */
export type RunOptions = { config: string | null, preset: string | null, on_conflict: string | null, recursive: boolean | null, output_dir: string | null, output_template: string | null, max_depth: number | null, report: string | null, format: string | null, block_size: number | null, hunk_size: number | null, codecs: Array<string> | null, mode: string | null, parent: string | null, full: boolean | null, fix: boolean | null, level: number | null, chunk_size: number | null, skip_verify: boolean | null, deep: boolean | null, deep_verify: boolean | null, algo: string | null, allow_encrypted: boolean | null, content_hashes: boolean | null, compress: boolean | null, cleanup: boolean | null, ensure_ticket_exists: boolean | null, decrypt: boolean | null, output_dir_cia: string | null, keys: string | null, block_size_exp: number | null, key: string | null, extensions: string | null, playlist_mode: string | null, api_base: string | null, input_checksum_min: string | null, input_checksum_max: string | null, inputs: Array<WupTitleInputOption> | null, platform: string | null, dat_id: string | null, dat_name: string | null, subset: string | null, skip_space_check: boolean | null, verify_after: boolean | null, quick: boolean | null, skip_probe: boolean | null, media_patch: boolean | null, title: string | null, };

/**
 * Result of a report-capable command. The message drives the operation log;
 * the records are accumulated client-side and handed back to
 * `cmd_write_report` once the run finishes, matching how the CLI collects
 * records and writes a single report at the end. A recursive run returns one
 * record per file, so the report keeps every row. `input_bytes`/`output_bytes`
 * are always populated (independent of the report toggle) so the batch
 * completion notification can total up space saved without requiring the
 * user to turn reporting on.
 */
export type RunOutcome = { message: string, 
/**
 * The runner's [`RunStatus`] as an integer: a partial failure completes
 * the command but did not convert every file, so the frontend marks the
 * job failed rather than done.
 */
status: number, records: Array<ReportRecord>, input_bytes: number, output_bytes: number, comparison: ComparisonData | null, 
/**
 * The runner's operation-specific payload, handed to the frontend as is
 * so result cards read structured data instead of parsing a message.
 */
data: unknown, };

/**
 * Dry-run plan lines for an operation that plans to a list of actions.
 */
export type RunPlansData = { plans: Array<PlanLine>, };

/**
 * Deserialized JSON request: operation name, input/output paths, and
 * per-operation options.
 */
export type RunRequest = { schema: string | null, operation: string, input: string | null, output: string | null, config: string | null, preset: string | null, options: RunOptions, dry_run: boolean, };

/**
 * JSON-encoded result of one run: status, message, records, progress
 * events, and operation-specific data.
 */
export type RunResponse = { schema: string, ok: boolean, status: number, code: string, message: string, details?: string, totals?: ReportTotals, records?: Array<ReportRecord>, events?: Array<ProgressEvent>, data?: RunData, };

/**
 * One row emitted to a live consumer during a run: a finished file's
 * report record, or a dry-run plan line.
 */
export type RunRow = { "kind": "record" } & ReportRecord | { "kind": "plan" } & PlanLine | { "kind": "hash" } & HashRow | { "kind": "dat_match" } & DatMatchData | { "kind": "dat_scan" } & DatScanRow | { "kind": "dat_rename" } & DatRenameRowData;

/**
 * Hash-tree verification result for one Wii partition, from the `--full` pass.
 */
export type RvlPartitionVerify = { offset: number, partition_type: number, kind: string, clusters_checked: number, mismatched_clusters: number, 
/**
 * Clusters whose only failing sectors hold uniform filler
 * ciphertext (scrubbing); they do not fail the verify.
 */
scrubbed_clusters: number, 
/**
 * First few mismatching cluster indices, capped for the report.
 */
sample_bad_clusters: Array<number>, ok: boolean, note: string | null, };

/**
 * Outcome of a Wii disc verification pass.
 */
export type RvlVerifyResult = { game_id: string, 
/**
 * Present only for `.rvz` input.
 */
rvz_structure: RvzStructuralVerify | null, 
/**
 * Per-partition hash-tree results, `--full` only.
 */
partitions: Array<RvlPartitionVerify>, ok: boolean, };

/**
 * Result of verifying the three SHA-1 hashes an RVZ container stores over its
 * own metadata structs.
 */
export type RvzStructuralVerify = { file_head_hash_ok: boolean, disc_hash_ok: boolean, 
/**
 * `None` when the container declares no partition table (`n_part == 0`).
 */
part_hash_ok: boolean | null, 
/**
 * 1 = GameCube, 2 = Wii.
 */
disc_type: number, iso_size: number, n_part: number, };

/**
 * Distinguishes a plain Standard CIA from one that is Standard only
 * because its content was decrypted (which necessarily rewrites the
 * per-chunk hashes, the info-record hashes, and the
 * content_info_records_hash in the header, invalidating Nintendo's
 * signature). Detected by the Encrypted flag being clear on every
 * content chunk record.
 */
export type StandardSubType = "Encrypted" | "Decrypted";

/**
 * Per-title hash-check outcome.
 */
export type TitleVerdict = { title_id: number, title_id_hex: string, ok: boolean, 
/**
 * Contents whose SHA-1 matched the TMD hash.
 */
verified_content: number, 
/**
 * Contents whose SHA-1 differed from the TMD hash.
 */
mismatched_content: number, 
/**
 * Contents that could not be hash-checked (hashed-mode, unknown mode,
 * or no TMD as with `.wua`/loadiine).
 */
skipped_content: number, };

/**
 * Outcome of a post-conversion verification pass.
 */
export type VerifyReport = { ok: boolean, round_trip: boolean, message: string, };

/**
 * One Wii U title input: a bare path, or a path with an explicit format
 * and key.
 */
export type WupTitleInputOption = string | { path: string, format: string | null, key: string | null, key_path: string | null, };

/**
 * Overall verification outcome for one input.
 */
export type WupVerifyResult = { 
/**
 * Input kind: `"nus"`, `"disc"`, `"wua"`, or `"loadiine"`.
 */
kind: string, ok: boolean, titles: Array<TitleVerdict>, };

/**
 * Result of a `xenon.convert` run: the Games on Demand container written.
 */
export type XenonConvertData = { title_id: number, media_id: number, part_count: number, total_bytes: number, };

/**
 * Result of a `xenon.verify` run.
 */
export type XenonVerifyData = { blocks: number, logical_bytes: number, hash_ok: boolean, };
